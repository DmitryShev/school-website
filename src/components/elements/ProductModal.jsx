import React from 'react'
import { string } from 'prop-types'


export const ProductModal = ({ text, description }) => (
  
);

ProductModal.propTypes = {
  text: string.isRequired,
  description: string.isRequired
}